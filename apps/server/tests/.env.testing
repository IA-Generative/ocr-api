ENVIRONMENT=testing
ALLOW_SQLITE_FALLBACK=1

# LLM calls are mocked in tests; this just needs to satisfy OpenAISettings'
# fail-loud validation so services.factory.load_worker() can construct its
# OpenAI client without hitting the network.
OPENAI_API_BASE_URL=http://llm-hub.test/v1
