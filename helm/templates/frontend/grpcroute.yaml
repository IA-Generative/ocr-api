{{- if .Values.enabled }}
{{- if .Values.frontend.grpcRoute.enabled -}}
{{- $fullNameApp := printf "%s-%s" (include "helper.fullname" .) "frontend" -}}
{{- $svcPortApp := .Values.frontend.service.port -}}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "frontend" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "frontend")) | nindent 4 }}
  {{- if .Values.frontend.grpcRoute.labels }}
    {{- toYaml .Values.frontend.grpcRoute.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.frontend.grpcRoute.annotations }}
  annotations:
    {{- tpl (toYaml .Values.frontend.grpcRoute.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.frontend.grpcRoute.parentRefs }}
  parentRefs:
  {{- range .Values.frontend.grpcRoute.parentRefs }}
  - name: {{ .name }}
    {{- if .namespace }}
    namespace: {{ .namespace }}
    {{- end }}
    {{- if .sectionName }}
    sectionName: {{ .sectionName }}
    {{- end }}
    {{- if .port }}
    port: {{ .port }}
    {{- end }}
    {{- if .group }}
    group: {{ .group }}
    {{- end }}
    {{- if .kind }}
    kind: {{ .kind }}
    {{- end }}
  {{- end }}
  {{- end }}
  {{- if .Values.frontend.grpcRoute.hostnames }}
  hostnames:
  {{- range .Values.frontend.grpcRoute.hostnames }}
  - {{ . | quote }}
  {{- end }}
  {{- end }}
  {{- if .Values.frontend.grpcRoute.rules }}
  rules:
  {{- range .Values.frontend.grpcRoute.rules }}
  {{- if .matches }}
  - matches:
    {{- range .matches }}
    {{- if .method }}
    - method:
        {{- if .method.type }}
        type: {{ .method.type }}
        {{- end }}
        {{- if .method.service }}
        service: {{ .method.service }}
        {{- end }}
        {{- if .method.method }}
        method: {{ .method.method }}
        {{- end }}
    {{- end }}
      {{- if .headers }}
      headers:
      {{- range .headers }}
      - type: {{ .type | default "Exact" }}
        name: {{ .name }}
        value: {{ .value }}
      {{- end }}
      {{- end }}
    {{- end }}
    {{- end }}
    {{- if .filters }}
    filters:
    {{- toYaml .filters | nindent 4 }}
    {{- end }}
    {{- if .backendRefs }}
    backendRefs:
    {{- range .backendRefs }}
    - name: {{ .name | default $fullNameApp }}
      port: {{ .port | default $svcPortApp }}
      {{- if .weight }}
      weight: {{ .weight }}
      {{- end }}
      {{- if .group }}
      group: {{ .group }}
      {{- end }}
      {{- if .kind }}
      kind: {{ .kind }}
      {{- end }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
    {{- end }}
    {{- else }}
    backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
    {{- end }}
  {{- end }}
  {{- else }}
  {{/* Default rule when no rules specified - routes all gRPC traffic */}}
  rules:
  - backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
  {{- end }}
{{- end }}
{{- end }}
