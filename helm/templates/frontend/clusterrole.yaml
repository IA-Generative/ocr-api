{{- if .Values.enabled }}
{{- if and .Values.frontend.serviceAccount.enabled .Values.frontend.serviceAccount.clusterRole.create .Values.frontend.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "frontend" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "frontend")) | nindent 4 }}
rules: {{- toYaml .Values.frontend.serviceAccount.clusterRole.rules | nindent 0 }}
{{- end }}
{{- end }}
