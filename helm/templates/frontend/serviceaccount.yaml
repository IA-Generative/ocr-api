{{- if .Values.enabled }}
{{- if and .Values.frontend.serviceAccount.enabled .Values.frontend.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.frontend.serviceAccount.name | default (printf "%s-%s" (include "helper.fullname" .) "frontend") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "frontend")) | nindent 4 }}
  {{- if .Values.frontend.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.frontend.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.frontend.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
