{{- if .Values.enabled }}
{{- if .Values.worker.grpcRoute.enabled -}}
{{- $fullNameApp := printf "%s-%s" (include "helper.fullname" .) "worker" -}}
{{- $svcPortApp := .Values.worker.service.port -}}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "worker" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker")) | nindent 4 }}
  {{- if .Values.worker.grpcRoute.labels }}
    {{- toYaml .Values.worker.grpcRoute.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.worker.grpcRoute.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker.grpcRoute.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.worker.grpcRoute.parentRefs }}
  parentRefs:
  {{- range .Values.worker.grpcRoute.parentRefs }}
  - name: {{ .name }}
    {{- if .namespace }}
    namespace: {{ .namespace }}
    {{- end }}
    {{- if .sectionName }}
    sectionName: {{ .sectionName }}
    {{- end }}
    {{- if .port }}
    port: {{ .port }}
    {{- end }}
    {{- if .group }}
    group: {{ .group }}
    {{- end }}
    {{- if .kind }}
    kind: {{ .kind }}
    {{- end }}
  {{- end }}
  {{- end }}
  {{- if .Values.worker.grpcRoute.hostnames }}
  hostnames:
  {{- range .Values.worker.grpcRoute.hostnames }}
  - {{ . | quote }}
  {{- end }}
  {{- end }}
  {{- if .Values.worker.grpcRoute.rules }}
  rules:
  {{- range .Values.worker.grpcRoute.rules }}
  {{- if .matches }}
  - matches:
    {{- range .matches }}
    {{- if .method }}
    - method:
        {{- if .method.type }}
        type: {{ .method.type }}
        {{- end }}
        {{- if .method.service }}
        service: {{ .method.service }}
        {{- end }}
        {{- if .method.method }}
        method: {{ .method.method }}
        {{- end }}
    {{- end }}
      {{- if .headers }}
      headers:
      {{- range .headers }}
      - type: {{ .type | default "Exact" }}
        name: {{ .name }}
        value: {{ .value }}
      {{- end }}
      {{- end }}
    {{- end }}
    {{- end }}
    {{- if .filters }}
    filters:
    {{- toYaml .filters | nindent 4 }}
    {{- end }}
    {{- if .backendRefs }}
    backendRefs:
    {{- range .backendRefs }}
    - name: {{ .name | default $fullNameApp }}
      port: {{ .port | default $svcPortApp }}
      {{- if .weight }}
      weight: {{ .weight }}
      {{- end }}
      {{- if .group }}
      group: {{ .group }}
      {{- end }}
      {{- if .kind }}
      kind: {{ .kind }}
      {{- end }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
    {{- end }}
    {{- else }}
    backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
    {{- end }}
  {{- end }}
  {{- else }}
  {{/* Default rule when no rules specified - routes all gRPC traffic */}}
  rules:
  - backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
  {{- end }}
{{- end }}
{{- end }}
