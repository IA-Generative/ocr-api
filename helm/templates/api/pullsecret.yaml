{{- if .Values.enabled }}
{{- range $index, $value := .Values.api.imagePullSecrets }}
{{- if and $value.create $value.username $value.password }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helper.imagePullSecretName" (dict "root" $ "componentName" "api" "index" $index "value" $value) }}
  labels: {{- (include "helper.labels" (dict "root" $ "componentName" "api")) | nindent 4 }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "helper.imagePullSecret" dict "username" $value.username "password" $value.password "email" $value.email "registry" $value.registry }}
{{- end }}
{{- end }}
{{- end }}
