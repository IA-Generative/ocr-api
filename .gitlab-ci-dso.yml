include:
  - project: $CATALOG_PATH
    file:
      - vault-ci.yml
      - kaniko-ci.yml
      # Temporarily unused: `helm-package-push` below inlines a fixed copy of
      # `.helm:build-push` while the fix is validated here before being proposed
      # upstream. Restore the `extends` once the catalogue carries it.
      - helm-ci.yml
    ref: main

default:
  image: alpine:latest

variables:
  TAG: "${CI_COMMIT_REF_SLUG}"
  REGISTRY_URL: "${REGISTRY_HOST}/${PROJECT_PATH}"
  # The chart lives at the repository root (helm/Chart.yaml), not nested under a
  # charts/ parent - so CHARTS_DIR is "." (one level up from the chart itself),
  # same convention as CHARTS_DIR: ./charts pointing at charts/<name>/Chart.yaml
  # elsewhere.
  CHARTS_DIR: .

stages:
  - read-secret
  - docker-build
  - helm-package

read_secret:
  stage: read-secret
  extends:
    - .vault:read_secret

docker-build-ocr-api:
  stage: docker-build
  extends:
    - .kaniko:build-push
  variables:
    DOCKERFILE: "apps/server/ocr_backend/Dockerfile"
    WORKING_DIR: "."
    IMAGE_NAMES: ""  # Overridden in before_script
    EXTRA_BUILD_ARGS: "--build-arg DOCKERHUB_MIRROR_URL=${DOCKERHUB_MIRROR_URL}"
  rules:
    - when: always
  before_script:
    - |
      SHORT_SHA=$(echo $CI_COMMIT_SHA | cut -c1-7)
      # The DSO mirror only ever syncs branches, never tags (see dso-auto-sync.yaml),
      # so this pipeline never runs as a GitLab tag pipeline and $CI_COMMIT_TAG is
      # always empty here. appVersion is the app version bump/release-please already
      # committed to Chart.yaml before the sync ran - same source of truth
      # helm-package-push below reads for the chart's own version.
      APP_VERSION=$(grep '^appVersion:' helm/Chart.yaml | awk '{print $2}' | tr -d '"')
      if [ -z "$APP_VERSION" ]; then
        echo "Error: Could not extract appVersion from helm/Chart.yaml"
        exit 1
      fi
      TAGS="ocr-api:$SHORT_SHA"
      TAGS="$TAGS ocr-api:$CI_COMMIT_REF_SLUG"
      TAGS="$TAGS ocr-api:$APP_VERSION"
      if echo "$APP_VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
        TAGS="$TAGS ocr-api:latest"
      fi
      export IMAGE_NAMES="$TAGS"
      echo "Building with tags: $IMAGE_NAMES"

docker-build-ocr-worker:
  stage: docker-build
  extends:
    - .kaniko:build-push
  variables:
    DOCKERFILE: "apps/server/business/paddleocr2/Dockerfile"
    WORKING_DIR: "."
    IMAGE_NAMES: ""  # Overridden in before_script
    EXTRA_BUILD_ARGS: "--build-arg DOCKERHUB_MIRROR_URL=${DOCKERHUB_MIRROR_URL}"
  rules:
    - when: always
  before_script:
    - |
      SHORT_SHA=$(echo $CI_COMMIT_SHA | cut -c1-7)
      # See docker-build-ocr-api: this pipeline never runs as a GitLab tag pipeline,
      # so the version tag comes from the appVersion already committed to Chart.yaml.
      APP_VERSION=$(grep '^appVersion:' helm/Chart.yaml | awk '{print $2}' | tr -d '"')
      if [ -z "$APP_VERSION" ]; then
        echo "Error: Could not extract appVersion from helm/Chart.yaml"
        exit 1
      fi
      TAGS="ocr-service-paddle-2.10.0:$SHORT_SHA ocr-worker:$SHORT_SHA"
      TAGS="$TAGS ocr-service-paddle-2.10.0:$CI_COMMIT_REF_SLUG ocr-worker:$CI_COMMIT_REF_SLUG"
      TAGS="$TAGS ocr-service-paddle-2.10.0:$APP_VERSION ocr-worker:$APP_VERSION"
      if echo "$APP_VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
        TAGS="$TAGS ocr-service-paddle-2.10.0:latest ocr-worker:latest"
      fi
      export IMAGE_NAMES="$TAGS"
      echo "Building with tags: $IMAGE_NAMES"

docker-build-ocr-frontend:
  stage: docker-build
  extends:
    - .kaniko:build-push
  variables:
    DOCKERFILE: "./apps/client/Dockerfile"
    WORKING_DIR: "./apps/client"
    IMAGE_NAMES: ""  # Overridden in before_script
    EXTRA_BUILD_ARGS: "--build-arg DOCKERHUB_MIRROR_URL=${DOCKERHUB_MIRROR_URL}"
  rules:
    - when: always
  before_script:
    - |
      SHORT_SHA=$(echo $CI_COMMIT_SHA | cut -c1-7)
      # See docker-build-ocr-api: this pipeline never runs as a GitLab tag pipeline,
      # so the version tag comes from the appVersion already committed to Chart.yaml.
      APP_VERSION=$(grep '^appVersion:' helm/Chart.yaml | awk '{print $2}' | tr -d '"')
      if [ -z "$APP_VERSION" ]; then
        echo "Error: Could not extract appVersion from helm/Chart.yaml"
        exit 1
      fi
      TAGS="ocr-frontend:$SHORT_SHA"
      TAGS="$TAGS ocr-frontend:$CI_COMMIT_REF_SLUG"
      TAGS="$TAGS ocr-frontend:$APP_VERSION"
      if echo "$APP_VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
        TAGS="$TAGS ocr-frontend:latest"
      fi
      export IMAGE_NAMES="$TAGS"
      echo "Building with tags: $IMAGE_NAMES"

# Inlined copy of the catalogue's `.helm:build-push` (helm-ci.yml) with its chart
# detection fixed, so the fix can be exercised here before it is proposed upstream.
# Two bugs, both in the incremental branch:
#   - the grep hardcoded `^charts/` instead of honouring CHARTS_DIR, so it never
#     matched this repository's `helm/` chart;
#   - a commit touching no chart left that grep exiting 1, which `set -eo pipefail`
#     turned into a failed job instead of the intended clean skip.
helm-package-push:
  stage: helm-package
  only:
    - tags
    - main
    - dev
  image: alpine/helm:4.1.3
  script:
    # Install git for detecting changed charts
    - apk add --no-cache git
    # Detect changed charts based on git diff
    - |
      CHARTS_PREFIX=$(echo "${CHARTS_DIR}" | sed 's|^\./||; s|/$||')
      if [ -z "$CI_COMMIT_BEFORE_SHA" ] || [ "$CI_COMMIT_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
        echo "No previous commit found, packaging all charts"
        CHANGED_CHARTS=$(find ${CHARTS_DIR} -maxdepth 1 -type d ! -path ${CHARTS_DIR} -exec basename {} \;)
      else
        echo "Detecting changed charts between $CI_COMMIT_BEFORE_SHA and $CI_COMMIT_SHA"
        CHANGED_FILES=$(git diff --name-only ${CI_COMMIT_BEFORE_SHA} ${CI_COMMIT_SHA})
        echo "Changed files: $CHANGED_FILES"
        if [ "$CHARTS_PREFIX" = "." ] || [ -z "$CHARTS_PREFIX" ]; then
          # Charts sit at the repository root, so the chart directory is the first
          # path segment - same set the `find` branch above would return.
          CHANGED_CHARTS=$(echo "$CHANGED_FILES" | cut -d'/' -f1 | sort -u)
        else
          CHANGED_CHARTS=$(echo "$CHANGED_FILES" | grep "^${CHARTS_PREFIX}/" | cut -d'/' -f2 | sort -u || true)
        fi
      fi
      echo "Changed charts: $CHANGED_CHARTS"
    - |
      if [ -z "$CHANGED_CHARTS" ]; then
        echo "No charts changed, skipping..."
        exit 0
      fi
    # Setup Docker auth for Helm (same credentials as kaniko)
    - mkdir -p /root/.docker
    - echo "$DOCKER_AUTH" > /root/.docker/config.json
    # Package and push each changed chart
    - |
      for CHART in $CHANGED_CHARTS; do
        CHART_PATH="${CHARTS_DIR}/${CHART}"
        if [ ! -f "${CHART_PATH}/Chart.yaml" ]; then
          echo "Skipping ${CHART} - not a valid chart"
          continue
        fi

        # Extract version from Chart.yaml
        CHART_VERSION=$(grep '^version:' ${CHART_PATH}/Chart.yaml | awk '{print $2}')
        CHART_NAME=$(grep '^name:' ${CHART_PATH}/Chart.yaml | awk '{print $2}')

        # Validate extracted values
        if [ -z "$CHART_VERSION" ] || [ -z "$CHART_NAME" ]; then
          echo "Error: Could not extract chart name or version from ${CHART_PATH}/Chart.yaml"
          exit 1
        fi

        echo "Packaging ${CHART_NAME} version ${CHART_VERSION}"

        # Update dependencies if Chart.lock exists or dependencies are defined
        if [ -f "${CHART_PATH}/Chart.lock" ] || grep -q "^dependencies:" "${CHART_PATH}/Chart.yaml"; then
          echo "Updating chart dependencies..."
          helm dependency update ${CHART_PATH}
        fi

        # Package the chart
        helm package ${CHART_PATH}

        # Push to Harbor as OCI artifact
        helm push ${CHART_NAME}-${CHART_VERSION}.tgz oci://${IMAGE_REPOSITORY}

        echo "Successfully pushed ${CHART_NAME}:${CHART_VERSION} to ${IMAGE_REPOSITORY}"
      done
